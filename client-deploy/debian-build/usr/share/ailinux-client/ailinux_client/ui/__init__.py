"""
AILinux Client UI Components

Note: Components are NOT auto-imported to avoid circular imports
and to allow login dialog to load before main window.

Use explicit imports:
    from .ui.main_window import MainWindow
    from .ui.login_dialog import LoginDialog
"""
