# --- HTTP :80 -> HTTPS Redirect (forum) ---
<VirtualHost *:80>
  ServerName forum.ailinux.me

  RewriteEngine On
  RewriteRule ^/(.*)$ https://forum.ailinux.me/$1 [R=301,L]

  Header always set X-Content-Type-Options "nosniff"
</VirtualHost>

# --- HTTPS :443 reverse proxy to Flarum ---
<VirtualHost *:443>
  ServerName forum.ailinux.me

  SSLEngine on
  SSLCertificateFile      /etc/letsencrypt/live/ailinux.me/fullchain.pem
  SSLCertificateKeyFile   /etc/letsencrypt/live/ailinux.me/privkey.pem

  IncludeOptional conf.d/snippets/ssl-params.conf

  ProxyPreserveHost On
  ProxyRequests Off
  ProxyPass / http://flarum:8888/
  ProxyPassReverse / http://flarum:8888/
  RequestHeader set X-Forwarded-Proto "https"
  RequestHeader set X-Forwarded-Port "443"
  RequestHeader set X-Forwarded-Ssl "on"

  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"

  Protocols h2 http/1.1
  SSLVerifyClient none
</VirtualHost>
