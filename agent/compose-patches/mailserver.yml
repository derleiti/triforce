# === PATCH für mailserver/docker-compose.yml ===
# 1. Füge diesen Service-Block am Ende der services: Sektion hinzu:

  codex-agent:
    build:
      context: ../agent
      dockerfile: Dockerfile
    container_name: mail_codex_agent
    environment:
      TRIFORCE_ROOT: /opt/triforce
      MCP_ENDPOINT: http://host.docker.internal:9100/v1/mcp
      CHECK_INTERVAL: "60"
      TZ: Europe/Berlin
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - mail_network

# 2. Falls noch kein networks: Block existiert, füge hinzu:
networks:
  mail_network:
    driver: bridge

# 3. Falls der mailserver Service noch kein network hat:
#    Füge beim mailserver Service hinzu:
#    networks:
#      - mail_network
