[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (may need Redis/HTTP)
    slow: Slow tests (network calls, large data)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
addopts = -v --tb=short
